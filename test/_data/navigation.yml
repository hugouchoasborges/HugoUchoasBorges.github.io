header:
  - titles:
      # @start locale config
      pt      : &PT       Arquivo
      en      : &EN       Archive
      # @end locale config
    url: /archive.html

  - titles:
      # @start locale config
      pt      : &PT       Sobre
      en      : &EN       About
      # @end locale config
    url: /about.html
